# Web_Bot_Menagement

apt update && apt upgrade -y && apt install python3 python3-pip -y && pip3 install python-telegram-bot==13.15 && bash <(curl -fsSL https://raw.githubusercontent.com/Baegyee9/web-bot/main/udp.sh)
